import React from 'react'

export default function index() {
  /* const date = new Date(createdAt)
  const formatDate = date.toLocaleDateString("en-US", {day: 'numeric', year: 'numeric', month: 'long' }) */
  return (
    <div>index</div>
  )
}
